name: Trigger auto deployment for jarvis-app

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/jarvis-app-AutoDeployTrigger-1faf2e5c-2267-44c8-b33a-3dccbe580bd2.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.JARVISAPP_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: hub.docker.com
          registryUsername: ${{ secrets.JARVISAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.JARVISAPP_REGISTRY_PASSWORD }}
          containerAppName: jarvis-app
          resourceGroup: jarvis-rg
          imageToBuild: hub.docker.com/jarvis-app:${{ github.sha }}
          dockerfilePath: Dockerfile

     

